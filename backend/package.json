{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "concurrently \"nodemon ./server/server.js\" \"clear\"",
    "db": "concurrently \" json-server -w ./db/db.json --routes ./db/routes.json \" \"clear\""
  },
  "keywords": [],
  "author": "",
  "main": "index.js",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "json-server": "^0.16.1",
    "nodemon": "^2.0.4"
  }
}
