{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "python ./server/api.py",
    "start:db": "nodemon -e js --ignore db/db.json ./db/index.js || clear",
    "start:unsecure-db": "json-server --watch ./db/db.json "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "basic-auth": "^2.0.1",
    "concurrently": "^5.2.0",
    "dotenv": "^8.2.0",
    "dotenv-expand": "^5.1.0",
    "json-server": "^0.16.1",
    "nodemon": "^2.0.4"
  },
  "nodemonConfig": {
    "events": {
      "start": "clear"
    }
  }
}
